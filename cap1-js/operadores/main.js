// bitwise
console.log(5 + 1)

let a = 5; // 5 em binário é 101
let b = 3; // 3 em binário é 011
let c = a & b; // c em binário é 001
console.log(c); // 1

/*
 * Neste exemplo,
 * o operador bitwise AND é usado para comparar os bits de a e b.
 * O resultado é c, que tem um 1 apenas na posição onde ambos a e b têm um 1.
 * O valor de c é, portanto, 1.
 */
